//whatap.conf를 다른 것으로 사용하고 싶을때 
process.env.WHATAP_CONF = "whatap"; 
process.env.WHATAP_NAME = "NODE-{ip2}-{ip3}"

//플러그인추가
var WhatapAgent = require('whatap').NodeAgent;
WhatapAgent.plugin("extra", function(pack){
	pack.putInt(9001,41);
});

